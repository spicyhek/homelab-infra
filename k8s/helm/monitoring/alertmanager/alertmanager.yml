global:
  resolve_timeout: 5m

route:
  receiver: discord
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: discord
    webhook_configs:
      - url: 'http://alertmanager-discord.monitoring.svc.cluster.local:8080/alert'
        send_resolved: true
